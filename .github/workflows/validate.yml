name: HACS Validate
on:
  push:
  pull_request
jobs:
  validate-hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: HACS validation
        uses: hacs/action@main
        with:
          category: integration
          # you can add: ignore: "brands" to skip brand check, etc.

